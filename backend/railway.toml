# Railway config-as-code: https://docs.railway.com/deploy/config-as-code
# Schema: applied automatically on first API startup when tables are missing (see app/schema_bootstrap.py).
# Optional manual run: python scripts/migrate.py
