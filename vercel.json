{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix server",
  "buildCommand": "npm run build --prefix server && mkdir -p server/public",
  "outputDirectory": "server/public"
}
