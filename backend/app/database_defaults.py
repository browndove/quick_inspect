"""
Fallback Postgres URL when the DATABASE_URL environment variable is not set.

Set your connection string here for local or fixed deployments. Prefer
DATABASE_URL on Railway/CI so secrets are not committed.
"""

DEFAULT_DATABASE_URL = ""
